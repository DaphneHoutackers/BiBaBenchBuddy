name: Build and Release Desktop Apps

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build_release:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Dependencies
        run: npm install

      - name: Build Electron App
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run app:build

      # Rename .dmg/.exe to include tag and to fixed name as extra copy
      - name: Prepare artifacts for release (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          tag="${GITHUB_REF##*/}"
          for file in release/*.dmg; do
            [ -e "$file" ] || continue
            cp "$file" "release/BiBaBenchBuddy-mac-arm64.dmg"
            mv "$file" "release/BiBaBenchBuddy-${tag}-mac-arm64.dmg"
          done

      - name: Prepare artifacts for release (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          tag="${GITHUB_REF##*/}"
          for file in release/*.exe; do
            [ -e "$file" ] || continue
            cp "$file" "release/BiBaBenchBuddy-Setup.exe"
            mv "$file" "release/BiBaBenchBuddy-${tag}-Setup.exe"
          done

      - name: Upload macOS artifact
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: desktop-build-macos
          path: release/*.dmg

      - name: Upload Windows artifact
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: desktop-build-windows
          path: release/*.exe

  create_release:
    needs: build_release
    runs-on: ubuntu-latest

    steps:
      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: desktop-build-macos
          path: release/macos

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: desktop-build-windows
          path: release/windows

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            release/macos/*.dmg
            release/windows/*.exe
